# databricks.yml (VERSÃO FINAL COM TODOS OS SCRIPTS PADRONIZADOS PARA .py)
bundle:
  name: databricks_hotel_etl

workspace:
  host: https://dbc-03ce1db1-207c.cloud.databricks.com

sync:
  include:
    - "src/**/*"

resources:
  pipelines:
    dlt_hotel_pipeline:
      name: "DLT - Pipeline de ETL da Hotelaria"
      catalog: "production"
      target: "trusted"
      serverless: true
      libraries:
        - notebook:
            # MUDANÇA: Atualizamos o caminho para o ficheiro .py
            path: "src/dlt_pipelines/dlt_raw_pipeline.py"
        - notebook:
            path: "src/dlt_pipelines/dlt_trusted_pipeline.py"
      
      development: true
      continuous: false

targets:
  dev:
    default: true